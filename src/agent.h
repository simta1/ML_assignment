#ifndef AGENT_H
#define AGENT_H

#include <bits/stdc++.h>
#include "move.h"
using namespace std;

class Agent {
private:

public:
    Agent(const string &path) {
        // TODO
    }

    Move predict(const string &state) {
        return Move::LEFT; // TODO
    }
};

#endif